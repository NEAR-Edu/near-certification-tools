# https://nextjs.org/docs/basic-features/environment-variables#default-environment-variables
# Note: .env, .env.development, and .env.production files should be included in your repository as they define defaults. .env*.local should be added to .gitignore, as those files are intended to be ignored. .env.local is where secrets can be stored.
#~~~~~~~~~~~~~~~~~~~~~~~~~~~
# Must be associated with the Authority AccountID below. Found in the JSON file in ~/.near-credentials/
ISSUING_AUTHORITY_PRIVATE_KEY=
# E.g. We require this API key to exist in headers of 'post' requests so that nobody unauthorized can mint certificates. It can be any arbitrary random value. We need to give this API key to Integromat:
API_KEY=
# 6 hours:
DYNAMIC_CERT_IMAGE_GENERATION_CACHE_SECONDS=21600
#~~~~~~~~~~~~~~~~~~~~~~~~~~~
NEXT_PUBLIC_BASE_URL=http://localhost:3000
NEXT_PUBLIC_CERTIFICATE_CONTRACT_NAME=
NEXT_PUBLIC_ISSUING_AUTHORITY_ACCOUNT_ID=
NEXT_PUBLIC_GAS=
NEXT_PUBLIC_NEAR_NETWORK_ID=
NEXT_PUBLIC_NEAR_NODE_URL=
NEXT_PUBLIC_NEAR_WALLET_URL=
NEXT_PUBLIC_NEAR_HELPER_URL=
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=
#~~~~~~~~~~~~~~~~~~~~~~~~~~~
# https://vercel.com/guides/nextjs-prisma-postgres
# This was inserted by `prisma init`:
# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB (Preview) and CockroachDB (Preview).
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# DATABASE_URL="postgres://public_readonly:nearprotocol@mainnet.db.explorer.indexer.near.dev/mainnet_explorer"
DATABASE_URL="postgres://public_readonly:nearprotocol@testnet.db.explorer.indexer.near.dev/testnet_explorer"
#~~~~~~~~~~~~~~~~~~~~~~~~~~~
